
<!--.col-	.col-sm-	.col-md-	.col-lg-	.col-xl- -->
{{!-- to show ALL accounts in the database --}}
<style>
    .table td.fit, 
    .table th.fit {
    white-space: nowrap;
    width: 1%;
    }
</style>

<div class="">
    <table id="accountsList" data-toggle="table" data-pagination="true" data-url="/staff/accounts-data" data-search="true" data-sort-name="type" data-show-refresh="true" data-show-columns="true" data-search-on-enter-key="true" data-show-search-button="true">
        <thead>
            <th data-field="type" data-sortable="true">Type</th>
            <th data-field="fname" data-sortable="true">First Name</th>
            <th data-field="lname" data-sortable="true">Last Name</th>
            <th data-field="email">Email</th>
            <th data-field="hp">H/P</th>
            <th></th>
            <th></th>
            <th></th>
        </thead>
    </table>



    {{!-- {{#if accounts}}
    <h1 class="text-muted">Accounts</h1>
    <hr>
    <div>
        <table id="accountsList" data-toggle="table" data-pagination="true" data-search="true">
            <thead>
                <tr>
                    <th data-sortable="true">Type</th>
                    <th data-sortable="true">First Name</th>
                    <th data-sortable="true">Last Name</th>
                    <th class="th-sm">Email</th>
                    <th class="th-sm">HP</th>
                    <th class="th-sm"></th>
                    <th class="th-sm"></th>
                    <th class="th-sm"></th>
                </tr>
            </thead>
            <tbody>
                {{#each accounts}}
                    <tr>
                        <td>{{type}}</td>
                        <td>{{fname}}</td>
                        <td>{{lname}}</td>
                        <td>{{email}}</td>
                        <td>{{hp}}</td>
                        <td><a href="/staff/manageAccount/{{id}}" class="btn"><i class="fas fa-pencil-square-o"></i></a></td>
                        <td>
                            <a href="/staff/deleteStaff/{{id}}" class="btn"><i class="fas fa-trash"></i></a></td>
                        </td>
                        <td>
                            <a href="/staff/staffPDF/{{id}}" class="btn" target="_blank"><i class="fas fa-print"></i></a>
                        </td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    {{else}}
        <h3>No accounts registered.</h3>
    {{/if}} --}}

</div>

<script>
    $('#datepicker').datepicker({
        format: 'yyyy-mm-dd',
        uiLibrary: 'bootstrap4',
        defaultDate: "{{formatDate content.dob 'YYYY-MM-DD'}}"
    $(function() {
    $accountsList.bootstrapTable({
      columns: [{
        title: 'ID',
        field: 'id'
      }, {
        title: 'Item Name',
        field: 'name'
      }, {
        title: 'Item Price',
        field: 'price'
      }]
    })
  })
</script>
