<div class="container">
{{#if orders}}
<div class="card card-body">
<h3 class="text-primary">My Orders</h3>
    {{#each orders}}
{{!—- orders info --}}
</div>
    {{/each}}
</div>
{{else}}
    <h3>No orders have been made</h3>
{{/if}} 