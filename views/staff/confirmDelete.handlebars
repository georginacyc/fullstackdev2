<div class="row">
    <div class="col-md-6 mx-auto">
        <div class="card card-body">
            <h3 class="text-muted text-center">Are you sure you want to delete this user?</h3>

            <p>Type: {{user.type}}</p>
            <p>Name: {{user.fname}} {{user.lname}}</p>
            <p>Gender: {{user.gender}}</p>
            <p>DOB: {{user.dob}}</p>
            <p>Email: {{user.email}}</p>
            <p>H/P: {{user.hp}}</p>
            <p>Postal Code: {{user.postalcode}}</p>
            <p>Address: {{user.address}}</p>
            <hr>
            <p>THIS ACTION IS IRREVERSIBLE. Please double-check.</p>
            <div class="row">
                <div class="col-12 col-sm-12 col-md-12 col-lg-12">
                    <a class="btn btn-danger float-right" href="/staff/admin/delete-user/{{user.id}}">Delete</a>
                </div>
            </div>
            
        </div>
    </div>
</div>